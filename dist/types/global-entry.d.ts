import * as DomUtils from './dom-utils';
/**
 * DOM Utils - A lightweight TypeScript library for DOM manipulation and traversal
 * @module dom-utils
 */
/**
 * Export all utility functions as a namespace for global usage
 * This will be available as the global variable 'DomUtils' when used via script tag
 */
export default DomUtils;
